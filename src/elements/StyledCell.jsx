import { css} from "styled-components";

export const StyledCell = css`
    padding: 1.6rem 1.6rem 1.6rem 0;
    margin: 0 0 0 1.6rem;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    border-bottom: 0.09rem solid var(--light);
`